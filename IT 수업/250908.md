<h5>
4장 조건문, 반복문
</h5>

```java
// if-elseif 
if () {
    
} else if {
    
} else {
    
}
```

```java
// swtich-case

switch (조건변){    // byte, short, int, char, String, enum, Wrappler 클래 만 올 수 있다. 
                   // long이나 실수 type은 못씀
    case 값1: 수행문;
              break;
    case 값2: 수행문;
              break;
    default: 
}
```
